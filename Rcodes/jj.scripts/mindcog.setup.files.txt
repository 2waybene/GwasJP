## Set up ACR and APOE model file and pheno files for all 4 paths
python bin/jj.scripts/launch.mindcog.interaction.model.setup.py "${pathList[@]}"

## Run common variant analysis w/ interaction APOE
python bin/jj.scripts/launch.mindcog.apoe.interaction.analysis.py "${pathList[@]}"

## Run imputed variant analysis w/ interaction 
python bin/jj.scripts/launch.mindcog.apoe.interaction.analysis.imputed.py "${pathList[@]}"

## Run common variant analysis w/ interaction ACR
python bin/jj.scripts/launch.mindcog.acr.interaction.analysis.py "${pathList[@]}"

## Run common variant analysis w/ interaction ACR
python bin/jj.scripts/launch.mindcog.acr.interaction.analysis.imputed.py "${pathList[@]}"

## Merge all chunks into chrs
python bin/jj.scripts/launch.mindcog.merge.chunks.py "${pathList[@]}"

## Merge chrs into single file
python bin/jj.scripts/launch.mindcog.merge.chrs.py "${pathList[@]}"
